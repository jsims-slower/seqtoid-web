require 'rails_helper'

RSpec.describe UserStorageConsumptionSnapshot, type: :model do
  # TODO: Add gem shoulda-matchers to test validations

  # subject { build(:user_storage_consumption_snapshot) }

  # it { should validate_presence_of(:snapshot_date) }
  # it { should validate_presence_of(:total_users) }
  # it { should validate_presence_of(:total_samples) }
  # it { should validate_presence_of(:total_input_files) }
  # it { should validate_presence_of(:total_input_files_size) }

  # it { should validate_uniqueness_of(:snapshot_date) }
end
