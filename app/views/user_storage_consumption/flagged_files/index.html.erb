<%= javascript_tag do %>
  react_component(
    "UserStorageConsumptionFlaggedFiles",
    {
      flaggedFiles: JSON.parse('<%= raw escape_json(@flagged_files) %>'),
      thresholds: JSON.parse('<%= raw escape_json(@thresholds) %>'),
      summary: JSON.parse('<%= raw escape_json(@summary) %>')
    },
    'page_content'
  );
<% end %>
